{
  "name": "@momentia/firebase",
  "version": "0.1.0",
  "private": true,
  "description": "Momentia Firestore rules, indexes, and local emulator tooling",
  "scripts": {
    "emulate": "firebase emulators:start --project demo-momentia",
    "emulate:headless": "firebase emulators:exec --project demo-momentia --only firestore \"echo emulators up\"",
    "test:rules": "firebase emulators:exec --project demo-momentia --only firestore \"node scripts/test-rules.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase-admin": "^13.0.0",
    "firebase-tools": "^13.30.0"
  }
}